<!DOCTYPE html>
    <head>
        <title>Rocket.Q - Entre ou Crie sua sala</title>
        <meta charset="UTF-8"> <!-- habilitando os caracteres especiais da lingua portuguesa -->
        <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!-- tag meta para compatibilidade com o internet explorer --> 
        <!-- view port -->
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- CSS -->
        <link rel="stylesheet" href="/styles/global.css">
        <link rel="stylesheet" href="/styles/home.css">
        <!-- fontes usadas no projeto, é só ir no google fonts achar a fonte q tu quer, e eou tu importa no css ou cria esse link rel no html -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400&family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
    </head>
    <body>
        <div id="home" class="content">
            <header>
                <a href="/">
                    <img src="/images/logo-rocketq.svg" alt="Rocket.Q Logo">
                </a>
            </header>

            <div id="bg">
                <div class="ball top"></div>
                <div class="ball bottom"></div>
            </div>
    
            <main>
                <div class="container"><!-- .container cria automaticamente uma div com uam class container, #container cria automaticamente uma div com um id container -->
                    
                    <!-- tudo q está dentro do container eu vou remover e colocar em outro arquivo-->
                    <!-- eu tenho um index, q é igual para os dois (create-pass, e enter-rrom) porém eles tem miolinhos diferentes, vamos fazer de uma forma, que vamo renderizar o conteúdo dos nosso outros arquivos aq -->
                    <!-- vai renderizar a parte correta de acordo com a nossa url -->
                    <!-- vamos fazer isso usando ejs, e indo lá na nossa route -->

                    <!-- para colocarmos código js precisando colocar dentro das porcentagens -->
                    <!-- vamos incluir, oq queremos aq dentro -->
                    <!-- agr vamos fazer um if, para verificar qual a variável q estamos mandando para esse arquivo index -->
                    <!-- como o nome da nossa variável, do arquivo rote, é o mesmo nome dos nossos arquivos, podemos tranformar tudo isso include('parts/enter-room') em template string, e dai passar a variável page -->
                    <!-- se for enter room, ele vai renderizar a enter room se for create pass, vai renderizar o create pass -->
                    <%- include(`parts/${page}`) %>
                    <!-- só isso aq já faz o nosso conteúdo ser exibido certinho sem nenhum código repetido -->


                </div> 
            </main>
        </div>
    </body>
</html>